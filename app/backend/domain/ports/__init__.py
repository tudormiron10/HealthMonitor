"""Domain ports — abstract interfaces (contracts) for infrastructure adapters.

Each port defines WHAT the application needs, without specifying HOW.
Concrete implementations live in infrastructure/.
"""
