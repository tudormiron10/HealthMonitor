[pytest]
# Async tests run without a per-test marker (every service method is async).
asyncio_mode = auto
# Run from app/backend; tests live under tests/.
testpaths = tests
# Put app/backend on sys.path so `application`/`core`/`domain`/`infrastructure`
# and the `tests` package import without installing the project.
pythonpath = .
